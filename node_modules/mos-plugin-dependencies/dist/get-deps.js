'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _keys = require('babel-runtime/core-js/object/keys');

var _keys2 = _interopRequireDefault(_keys);

exports.default = getDeps;

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var gh = require('github-url-to-object');
var path = require('path');

function getDeps(opts) {
  return (0, _keys2.default)(opts.deps || {}).map(function (depname) {
    var reqPath = opts.pkgRoot + '/node_modules/' + depname + '/package.json';
    var dep = require(path.resolve(reqPath));
    if (dep.repository && dep.repository.url && gh(dep.repository.url)) {
      dep.repository.url = gh(dep.repository.url).https_url;
    }
    return dep;
  });
}
module.exports = exports['default'];