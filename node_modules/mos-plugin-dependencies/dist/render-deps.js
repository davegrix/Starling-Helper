'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = renderDeps;

var _getDeps = require('./get-deps');

var _getDeps2 = _interopRequireDefault(_getDeps);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function renderDeps(opts) {
  var depDetails = (0, _getDeps2.default)(opts);

  if (!depDetails.length) return 'None';

  return depDetails.map(function (depDetails) {
    return '- [' + depDetails.name + '](' + getDepURL(depDetails) + '): ' + depDetails.description.trim();
  }).join('\n');
}

function getDepURL(depDetails) {
  if (depDetails.repository) return depDetails.repository.url;

  return 'https://npmjs.org/package/' + depDetails.name;
}
module.exports = exports['default'];